{
  "name": "express-starter",
  "version": "1.0.0",
  "description": "API Express minimale pour tester des pipelines CI/CD",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test"
  },
  "engines": { "node": ">=20" },
  "dependencies": {
    "express": "^4.21.0"
  }
}
